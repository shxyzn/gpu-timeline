# 대시보드 수동 기록 — v1.1.1

별도 백엔드와 GPU 서버 업데이트 없이 사이트에서 실험을 추가·삭제·복구합니다.
서버 실행 기록과 사이트 편집 기록을 각각 GitHub에 보관하고, 화면에서 합쳐서 표시합니다.

## 처음 한 번: 관리자 브라우저 연결

1. 사이트 오른쪽 위 **관리자 로그인**을 누릅니다.
2. 이 브라우저에서 사용할 비밀번호를 정하고 확인란에도 입력합니다.
3. [GitHub Fine-grained token 생성](https://github.com/settings/personal-access-tokens/new) 화면에서 `gpu-timeline` 저장소만 선택하고 **Contents: Read and write** 권한을 부여합니다. 토큰 이름은 구분용으로 자유롭게 정합니다. 서버 이름과 관계없습니다.
4. 생성한 토큰을 사이트의 **GitHub 연결 키**에 붙여넣고 **연결하고 로그인**을 누릅니다.

이후 같은 브라우저에서는 비밀번호만 입력합니다. 새로고침하거나 로그아웃하면 편집 모드가 잠깁니다.
**이 브라우저에 연결 기억**을 해제하면 키를 저장하지 않아 다음 접속에 다시 연결해야 합니다.
비밀번호를 잊었거나 토큰이 만료·폐기되면 **연결 다시 설정**에서 새로 연결합니다.
다른 관리자·다른 브라우저도 처음 한 번은 연결 키가 필요합니다.

비밀번호는 브라우저에 저장된 키를 여는 잠금입니다. 공용 계정 DB·회원가입·서버의 사용자 계정과 연동하지 않습니다.
실제 GitHub 저장 권한은 연결한 토큰으로 검증됩니다. 읽기 전용 토큰은 로그인이 되더라도 저장 요청이 거절됩니다.
연결 키는 해당 저장소의 Contents 쓰기 권한을 가지므로 관리자만 사용합니다. 파일 하나에만 제한된 권한은 아닙니다.

토큰은 AES-GCM으로 암호화해 해당 브라우저의 localStorage에만 보관합니다. 비밀번호에서 PBKDF2-SHA256(600,000회)으로 키를 유도하며 salt·IV를 매번 무작위 생성합니다.
원문 토큰·비밀번호를 코드, 상태 JSON, GitHub 커밋, URL에 쓰지 않습니다. 복호화한 토큰은 로그인한 페이지 메모리에만 두며 GitHub API로만 전송합니다.
이 기능은 공개 대시보드의 편집을 위한 것으로, 사이트 조회나 실험 데이터를 비공개로 바꾸지는 않습니다.

## 사용법

- **추가:** 관리자 로그인 → 실험 추가 → 서버·GPU·실험명·시간·상태 입력 → 실험 등록.
- **삭제:** 실험 막대 → 삭제. 모든 사용자의 대시보드에서 숨깁니다. 실제 실험, 프로세스, 서버 로그, 결과 파일은 건드리지 않습니다.
- **복구:** 기록 관리 → 삭제된 기록 → 복구. 서버에서 이미 보존 기간이 지난 기록은 삭제 상태를 풀어도 원본이 없어 표시되지 않을 수 있습니다.
- **기간 밖 수동 기록:** 기록 관리에서 찾고 삭제할 수 있습니다.

입력 시간은 브라우저의 시간대와 관계없이 **Asia/Seoul**입니다.
실행 중 기록은 예상 종료를 비우면 현재까지 표시합니다. 예정 기록은 예상 종료도 필요하고, 완료·실패·취소는 실제 종료가 필요합니다.
수동 기록에는 `[수동]` 표시가 붙습니다. 수동 상태는 자동으로 바뀌지 않으며 실제 실험 실행·종료 추적·GPU 예약을 수행하지 않습니다.
서버의 GPU 사용률과 점유 요약은 원본 수집 결과를 기준으로 합니다. 실행 중 기록을 숨겨도 GPU가 여유로 바뀌지 않습니다.
사이트가 만든 `server-x/web-…` ID는 사이트의 수동 기록 ID입니다. 현재 서버 수집기에는 내려가지 않으므로 `gputl update/finish`에 사용할 수 없습니다. 수집기가 만든 기존 `server-x/job-…` ID 동작은 그대로입니다.

## 저장 구조와 동기화

| 작성자 | status 브랜치 경로 | 역할 |
| --- | --- | --- |
| 서버 수집기 | `servers/server-x.json` | 서버가 올리는 GPU 상태·실험 원본 스냅샷 |
| 관리자 브라우저 | `dashboard/edits.json` | 수동 실험 목록·삭제 목록 |

`dist/config.json`의 `dashboard` 설정에 저장소·브랜치·경로를 지정합니다. 쓰기 경로는 코드에서 `status` 브랜치의 `dashboard/edits.json`으로 제한합니다.
최초 저장 시 파일을 생성하므로 빈 파일을 수동으로 만들 필요가 없습니다. `status` 브랜치는 기존 수집기 업로드로 생성되어 있어야 합니다.

조회자는 기존처럼 공개 raw JSON을 읽습니다. 저장한 브라우저에는 성공 응답 후 즉시 적용하며, 다른 사람에게는 화면 조회 주기(기본 60초)와 GitHub 캐시 지연 뒤 반영됩니다.
저장할 때마다 GitHub Contents API에서 최신 파일과 SHA를 읽고 요청 하나를 적용합니다. 충돌(409/422)은 다시 읽어서 최대 4회 시도합니다. 다른 관리자의 변경과 수집기 업로드가 겹쳐도 전체 상태 파일을 덮어쓰지 않습니다.
응답 유실 후 같은 추가 요청을 재시도하면 같은 ID를 사용하여 중복 등록을 방지합니다. 실패하면 입력을 유지하고 성공으로 표시하지 않습니다.
편집 문서의 revision으로 지연된 캐시 응답이 방금 저장한 내용을 되돌리지 않게 합니다. 수신 실패·잘못된 문서 형식은 표시하고 마지막 수신 내용을 유지합니다.
형식이 잘못되었거나 지원하지 않는 schema_version이면 저장하지 않습니다. 수동 기록 문서는 800 KB까지만 저장하도록 검사합니다.

**v1.1.1은 서버↔사이트 양방향 동기화를 추가하지 않습니다.** 대시보드 기록 관리에는 필요하지 않기 때문입니다.
서버 원본까지 수정하거나 웹에서 만든 기록을 `gputl`이 관리하게 하려면 별도 후속 기능이 필요합니다.

## 개발 검증

```bash
npm ci
npm test
npx playwright install chromium
npm run test:browser
```

브라우저 검증은 모든 GitHub 요청을 모의 응답으로 처리합니다. 실제 저장소·실험을 수정하지 않습니다.
로그인·비밀번호 오류·로그아웃·재접속, 수동 추가, 삭제·복구, 권한 오류, 지연된 캐시·수신 실패, 공유 조회, GPU 점유 유지, 한국시간과 모바일 화면을 확인합니다.
