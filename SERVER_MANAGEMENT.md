# 서버 관리 — v1.1.3

관리자 로그인 → 서버 상세 → **서버 관리**에서 서버별 조치와 복사할 명령어를 확인합니다. 기존 관리자 비밀번호와 GitHub 연결을 그대로 사용합니다.

## 화면 구성

- 서버 설치 버전과 대시보드 전체 버전, 마지막 수신 상태·시각.
- 아직 서버에 설치되지 않은 기능의 선택 업데이트 안내.
- 이 서버에만 필요한 작업이 있으면 전용 안내와 명령어.
- 설치 계정·Python·GPU·공용 연결 상태 확인.
- 정확한 빌드 커밋으로 수집기 업데이트.
- 최신 상태 업로드 요청과 수집기 재시작.

이 화면은 명령어를 안내·복사합니다. 버튼으로 SSH에 접속하거나 서버 명령을 실행하지 않으며 상태 브랜치와 실험 기록에도 쓰지 않습니다. 명령어는 해당 서버의 SSH 터미널에 붙여넣으세요. 설정 파일·토큰을 사이트에 입력할 필요는 없습니다.

## 업데이트 명령

현재 운영 중인 **공용 수집기**를 업데이트하는 명령입니다. 기존 서비스 설치 계정에서 실행하세요. 점검 명령의 `User`에 그 계정이 표시됩니다. 사용 중인 Python은 `ExecStart`에서 읽어 이어서 사용하므로 별도로 Conda를 바꾸지 않아도 됩니다.

업데이트 전 설치 계정과 `/etc/gpu-timeline/config.json`의 서버 ID를 확인합니다. 선택한 서버와 다르면 중단하며 다른 서버의 ID로 덮어쓰지 않습니다. 화면에서 지정한 Git 커밋과 `VERSION`을 확인한 뒤 기존 설치 프로그램을 실행합니다.

소스는 임시 폴더에 다운로드하고 끝나면 제거합니다. `~/gpu-timeline`과 실험 작업 폴더를 수정하지 않습니다. 설치 시 수집기는 재시작되지만 기존 설정·실험 기록은 유지되고 실제 GPU 작업을 종료하지 않습니다. 전송이 끝나기 전에는 화면이 이전 버전일 수 있습니다. `gputl status`의 업로드 결과를 확인하고 화면의 **수신 다시 확인**을 누르세요.

공용 서비스가 없거나 별도의 방식으로 수집기를 운영하면 먼저 [공용 설치 안내](SHARED_SETUP.md)를 따르세요. 이름이 비슷한 다른 서비스나 사용자별 수집기를 자동으로 변경하지 않습니다.

## 서버마다 다른 조치 추가하기

`dist/config.json`의 `servers` 배열에서 해당 서버의 `actions`에 추가합니다. `command`에 입력한 줄바꿈까지 그대로 표시하고 복사합니다. 예:

```json
{
  "id": "server-b",
  "name": "Server B",
  "file": "servers/server-b.json",
  "installed_version_baseline": "1.0.0",
  "actions": [
    {
      "title": "최근 수집 로그 확인",
      "description": "수신 지연이 계속되면 이 서버에서 최근 수집기 로그를 확인하세요.",
      "command": "sudo journalctl -u gpu-timeline-shared.service -n 30 --no-pager"
    }
  ]
}
```

처리한 전용 조치는 `actions`에서 제거합니다. 실제 설치 기능은 수집기 보고 버전에 따라 안내에서 자동으로 빠지지만, 임의의 명령어 실행 여부는 사이트에서 알 수 없습니다.

공통 서버 기능은 `server_management.releases`에 추가합니다. `version`은 그 기능이 처음 들어간 세 자리 버전이며 서버 배포용 별도 번호가 아닙니다. 관리자 지정 기준보다 새로운 기능만 안내하고 미래의 미배포 기능은 제외합니다.

안내는 공개 정적 파일이므로 관리자 로그인은 화면 접근을 위한 장치입니다. 명령어에 토큰·비밀번호를 넣지 마세요.
