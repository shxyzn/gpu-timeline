# 버전과 서버 배포

전체 버전은 **v1.1.2+커밋ID** 형태입니다. GitHub Pages 배포 시 실제 빌드 커밋을 자동으로 붙입니다.

v1.1.2는 실험 시간·이름·설명 수정과 서버 수집값 복원을 추가합니다. 기존 관리자 연결과 비밀번호를 그대로 사용하며 서버 업데이트가 필요 없습니다.

v1.1.1의 관리자 로그인·대시보드 수동 추가/삭제/복구는 **웹 배포만으로 적용**됩니다. 서버 수집기 변경과 설치는 필요 없습니다. [대시보드 편집 안내](DASHBOARD_EDITING.md)를 참고하세요.

| 구분 | 예 | 변경 기준 |
| --- | --- | --- |
| 큰 버전 | 1 | 큰 구조·호환성 변경 |
| 서버 배포 묶음 | 1.1 | 서버에 모아서 배포할 기능 묶음 |
| 보정 버전 | 1.1.0 | 같은 배포 묶음 안의 보정 |
| 가장 작은 구분 | +bc9744d | 실제 Git 커밋. 코드가 바뀔 때마다 달라짐 |

**서버는 큰 버전 또는 중간 번호가 바뀔 때 모아서 업데이트**합니다. 같은 배포 묶음 안에서 대시보드나 코드가 바뀌어 커밋이 달라도 매번 서버에 설치할 필요는 없습니다. 서버 업데이트는 관리자가 실행하며 웹 배포가 서버 설치를 수행하지 않습니다.

현재 준비된 서버 배포 묶음은 **v1.1**입니다. 서버 버전 보고, 웹·CLI 공용 실험 ID, 진행률 기록시각을 포함합니다. 원하는 시점에 각 서버의 기존 관리자 계정으로 다음을 한 번씩 실행합니다.

```bash
cd ~/gpu-timeline
git pull --ff-only
bash agent/install-shared.sh
/usr/local/bin/gputl status
```

웹의 진행률 기반 완료 예정시간과 버전 표시 영역은 웹 배포만으로 적용됩니다. 공용 ID를 서버 명령에 입력하는 기능과 정확한 진행률 기록시각은 해당 서버의 v1.1 설치 후 적용됩니다.

## 버전을 표시하는 근거

- 원본 릴리스 번호: 저장소 루트의 VERSION.
- 대시보드: Pages 빌드가 dist/version.json에 전체 버전·실제 커밋·빌드 시각을 기록합니다. 이 생성 파일은 커밋하지 않습니다.
- 서버: 설치 프로그램이 /opt/gpu-timeline/version.json에 **설치할 때의** 버전·커밋·설치 시각을 저장합니다. 설치된 수집기가 agent_version으로 보고합니다.
- 서버에서 git pull만 해도 설치 버전이 바뀌지는 않습니다. 설치된 코드와 재시작된 수집기가 기준입니다.
- 이전 수집기는 버전을 보고하지 않으므로 '설치 버전 미보고'로 표시합니다. 사이트 버전이나 마지막 업로드 커밋으로 설치 버전을 추측하지 않습니다.
- .dirty는 수정된 소스에서 설치·빌드했다는 뜻입니다. Git 정보가 없으면 +unknown으로 표시합니다.

## 다음 변경 시

1. 같은 서버 배포 묶음의 웹 수정·작은 패치는 중간 번호를 유지합니다. Git 커밋이 가장 작은 변경 이력을 구분합니다.
2. 다음 서버 배포 묶음을 준비할 때 VERSION의 중간 번호를 올리고 보정 번호는 0으로 시작합니다. 예: 1.1.0 → 1.2.0.
3. CHANGELOG.md에 웹에서 즉시 적용되는 내용과 서버 설치 후 적용되는 내용을 구분해 적습니다.
4. 이전 서버가 계속 데이터를 보내도 대시보드가 표시할 수 있도록 새 필드는 선택 사항으로 취급합니다.
5. 버전·ID·계정 권한·ETA 변경은 관련 테스트와 GitHub CI를 확인합니다.
