# 버전과 서버 업데이트

대시보드와 서버는 모두 **v1.1.3+커밋ID**처럼 같은 전체 버전 형식을 사용합니다. 별도의 `v1.1` 서버 배포 기준은 없습니다. Git 커밋은 같은 릴리스 안에서 실제 코드를 구분하는 가장 작은 식별자입니다.

- 대시보드는 웹에 배포한 버전을 표시합니다.
- 서버는 그 서버에 실제로 설치한 버전을 표시합니다. 웹 버전과 달라도 정상입니다.
- 사소한 웹 변경마다 서버를 업데이트하지 않습니다. 필요한 서버 기능이 모였을 때 관리자가 원하는 전체 버전을 설치합니다. 버전 번호의 특정 자리가 바뀌었는지로 배포를 강제하지 않습니다.

예: 대시보드 **v1.1.3**, 서버 A·B·C **v1.0.0** → 서버 B만 업데이트하면 서버 B는 **v1.1.3+설치커밋**, 나머지는 계속 **v1.0.0**입니다.

## 현재 서버의 기준 버전

관리자 요청에 따라 현재 운영 중인 server-a, server-b, server-c의 기존 설치를 **v1.0.0**으로 명명합니다. 이 값은 `dist/config.json`의 각 서버 `installed_version_baseline`에 명시합니다. 과거 커밋이나 실제 서버 파일을 변경하지 않습니다.

버전을 보내지 않는 기존 수집기에는 `v1.0.0 · 관리자 지정 기준`을 표시합니다. 업데이트된 수집기가 유효한 `agent_version`을 보내면 그 전체 버전과 커밋을 우선합니다. 기준값이 없는 새 서버나 잘못된 버전 보고는 버전 확인이 필요하다고 표시합니다. 웹 버전·업로드 커밋을 설치 버전으로 추측하지 않습니다.

## v1.1.3 사용 방법

1. 대시보드에서 관리자 로그인합니다.
2. 서버 상세에서 해당 서버의 **서버 관리**를 누릅니다.
3. 조치 설명과 현재 설치 버전을 확인합니다. 필요할 때 명령어를 복사해 해당 서버의 SSH 터미널에서 실행합니다.
4. 수집기가 업로드한 다음 **수신 다시 확인**을 누르면 보고된 설치 버전을 확인할 수 있습니다. 명령어를 복사하는 것만으로 적용 완료로 표시하지 않습니다.

서버 관리 화면 자체와 v1.1.1·v1.1.2의 실험 추가·삭제·수정 기능은 웹 배포만으로 적용됩니다. 현재 서버에 바로 업데이트할 필요는 없습니다.

선택 업데이트에는 아직 서버에 설치하지 않은 공용 실험 ID, 설치 버전 자동 보고, 진행률 기록시각 기능이 포함됩니다. 화면의 명령어는 대시보드와 같은 정확한 커밋을 임시 폴더에 받아 설치합니다. 기존 작업 저장소를 전환하거나 변경하지 않고, 기존 서비스의 Python을 그대로 사용합니다. 기존 설치 계정과 서버 ID가 다르면 설치 전에 중단합니다.

## 버전을 표시하는 근거

- 원본 릴리스: 저장소 루트 `VERSION`의 세 자리 버전.
- 대시보드: Pages 빌드가 `dist/version.json`에 전체 버전·빌드 커밋·빌드 시각을 기록합니다. 생성 파일은 커밋하지 않습니다.
- 서버: 설치 프로그램이 `/opt/gpu-timeline/version.json`에 설치 당시 전체 버전·커밋·설치 시각을 저장하고, 수집기가 `agent_version`으로 보고합니다.
- 새 버전 메타데이터에는 두 자리 `deployment_version`을 생성하지 않습니다. 이전 메타데이터에 그 값이 있어도 전체 `version`만 사용합니다.
- `git pull`만 해도 설치 버전이 바뀌지 않습니다. 설치된 코드와 재시작된 수집기가 기준입니다.
- `.dirty`는 수정된 소스에서 설치·빌드했다는 뜻입니다. Git 정보가 없으면 `+unknown`입니다. 설치 명령은 정확한 깨끗한 빌드 커밋을 확인했을 때만 제공하며 더 낮거나 같은 릴리스로의 자동 재설치를 안내하지 않습니다.
- 수신 지연·실패 시 버전도 마지막 보고값입니다. 서버 관리에 마지막 수신 시각과 상태를 함께 표시합니다.

## 다음 변경 시

1. `VERSION`과 `CHANGELOG.md`에 전체 릴리스 번호와 변경 내용을 기록합니다.
2. 웹 변경과 서버 설치가 필요한 기능을 구분해 적습니다. 웹 수정만으로 서버 설치를 요구하지 않습니다.
3. 새 서버 기능은 `dist/config.json`의 `server_management.releases`에 **최초로 포함된 전체 버전**과 설명을 추가합니다. 이미 설치된 기능은 다시 조치로 요구하지 않습니다.
4. 특정 서버에만 필요한 조치는 해당 서버의 `actions`에 추가합니다. [서버 관리 안내](SERVER_MANAGEMENT.md)를 참고하세요.
5. 기존 수집기·설정과 계속 호환되도록 새 필드는 선택 사항으로 유지하고 관련 검증을 실행합니다.
