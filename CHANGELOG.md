# 변경 이력

## v1.1.3 — 2026-09-15

웹 배포로 적용 (서버 설치 불필요):
- 관리자 로그인 후 각 서버 상세에서 서버 관리 창 제공. 서버별 조치·업데이트·점검·업로드·재시작 명령을 안내하고 복사.
- 업데이트 명령은 설치 계정·서버 ID를 확인하고 정확한 빌드 커밋을 임시 폴더에서 설치. 기존 작업 저장소·Python·설정·실험 기록 유지.
- 명령 복사와 적용 완료를 구분. 다음 수집기 보고로 설치 버전을 확인하며 수신 시각·지연 상태를 함께 표시.
- 대시보드·서버 모두 전체 세 자리 버전으로 통일. 두 자리 서버 배포 기준 제거. 사소한 웹 변경마다 서버 배포를 요구하지 않음.
- 현재 server-a·b·c의 기존 설치를 관리자 지정 기준 v1.0.0으로 명명. 실제 agent_version 보고가 있으면 그 전체 버전을 우선.
- config.json의 서버별 actions와 공통 서버 기능 목록으로 조치 문구·명령어 관리.

향후 서버 설치 시 적용:
- 새 버전 메타데이터에서 별도 deployment_version 필드 제거. 이전 설치 메타데이터는 전체 version으로 계속 읽음.
- 아직 설치하지 않은 공용 실험 ID·버전 보고·진행률 시각 기능은 원하는 시점에 함께 적용.

## v1.1.2 — 2026-09-15

웹 배포로 적용 (서버 설치 불필요):
- 실험 상세에 수정 버튼 추가. 수동 실험은 실험명·등록자·설명·시간·GPU·상태 수정 가능.
- 수집 실험은 실험명·등록자·설명·시작·예상 종료·실제 종료시간을 웹 표시값으로 수정. 실행 상태·GPU·진행률은 계속 서버 수집값을 따름.
- 바꾼 항목만 보관하여 새 서버 업로드에도 수정값 유지. 서버 수집값으로 되돌리기 지원.
- 수정 시 기존 실험 ID와 생성시각 유지. 건드리지 않은 시간의 초·밀리초와 자동 ETA 보존.
- 동시에 같은 항목을 수정하면 충돌 안내, 다른 항목 수정은 병합. 수정하지 않고 저장하면 불필요한 커밋 없음.
- 기록 관리에서 기간 밖 수동 실험과 웹에서 수정한 수집 기록 편집 가능.

## v1.1.1 — 2026-09-15

웹 배포로 적용 (서버 설치 불필요):
- 관리자 브라우저 최초 GitHub 연결 후, 설정한 비밀번호로 편집 모드 로그인.
- 대시보드에서 서버·GPU를 선택해 수동 실험 추가, 실험 상세에서 삭제, 기록 관리에서 복구.
- status/dashboard/edits.json에 수동 기록과 숨김 목록을 저장. 서버 업로드 파일을 수정하지 않아 다음 업로드에도 숨김 유지.
- 원본 GPU 점유·사용률은 삭제 여부와 독립적으로 표시. 실제 프로세스·서버 로그에 영향 없음.
- 최신 SHA를 이용한 충돌 재시도, 추가 요청 중복 방지, 오래된 캐시 응답으로 화면이 되돌아가는 문제 방지.
- 수동 실험의 한국시간 입력, 예상 종료 미등록 시 현재까지 표시, 기간 밖 수동 기록 관리.
- 별도 백엔드·서버 양방향 통신 없음. 웹 수동 ID는 서버 gputl 관리 대상에 포함되지 않음.

관리자 연결·사용법: [대시보드 편집 안내](DASHBOARD_EDITING.md).

## v1.1.0 — 2026-09-14

웹 배포로 적용:
- 대시보드에 전체 버전과 실제 빌드 커밋 표시.
- 서버 상세에 각 수집기가 보고한 설치 버전 표시. 이전 수집기는 미보고로 표시.
- 진행률과 경과시간으로 완료 예정시간 추정, 타임라인·서버 상세·실험 상세에 반영.
- 직접 입력한 예상 종료시간 우선. 0%·100%·종료 결과 미확인 상태를 자동 완료로 처리하지 않음.
- 기존 진행률 데이터는 수신시각을 기준으로 대략 계산한다는 설명 표시.

서버 v1.1.0 이상 설치 후 적용:
- 설치 당시 버전·커밋·설치 시각을 보존하고 상태 JSON과 gputl status로 보고.
- gputl list 및 등록 응답에 사이트와 같은 reference(서버/공개 실험 ID) 제공.
- update·finish --id에 사이트의 공유 ID 사용 가능. 기존 명령어용 ID와 계정별 수정 권한 유지.
- 진행률 변경 시 실제 기록시각을 저장. 실험 재시작 시 이전 진행률 제거.

서버 설치는 관리자가 원하는 시점에 한 번씩 수행합니다. GitHub·웹 업데이트가 GPU 서버에 자동 배포되지는 않습니다.
